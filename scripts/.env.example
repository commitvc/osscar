# Copy to scripts/.env and fill in the real values.
# scripts/.env is gitignored.

# https://<project-ref>.supabase.co
SUPABASE_URL=https://eoblveaskkjopnlfkwtf.supabase.co

# Project Settings → API → service_role (secret). NEVER commit or ship to the browser.
SUPABASE_SERVICE_ROLE_KEY=
